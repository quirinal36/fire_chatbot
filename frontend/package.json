{
  "name": "fire-chatbot",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "소방본부 AI 상담 챗봇 — 도면·소방법·신규 건축물 문의 UI",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/three": "^0.186.0",
    "typescript": "^7.0.0",
    "vite": "^8.0.0",
    "vitest": "^5.0.1"
  },
  "dependencies": {
    "@supabase/auth-js": "^2.116.0",
    "three": "^0.186.0"
  }
}
