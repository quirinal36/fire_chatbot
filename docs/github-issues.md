# GitHub 이슈 백로그

원본: [기획서 v0.1](../소방시설_법령_API_챗봇_프로그램_기획서.md) · 상위 계획: [마일스톤](github-milestones.md) · 작성일: 2026-09-17

등록 완료: GitHub 이슈 #1~#32. 실제 이슈·마일스톤 링크는 [GitHub 등록 결과](github-registration.md)를 확인한다. GitHub 본문의 선행 작업은 실제 이슈 번호로 연결했다.

각 `ISS-*` 절을 독립 GitHub 이슈로 등록한다. 절 제목은 이슈 제목, 메타데이터 다음 문장은 작업 범위, 체크리스트는 완료 조건이다. 선행 작업은 완료에 필요한 의존성으로, 설계·fixture 준비는 먼저 진행할 수 있다. 모든 이슈는 미착수이며 담당자 미지정이다.

추천 labels: `priority:P0`, `priority:P1`; `type:decision`, `type:spike`, `type:feature`, `type:test`, `type:docs`, `type:ops`; `area:platform`, `area:ingestion`, `area:data`, `area:search`, `area:chat`, `area:rules`, `area:admin`, `area:security`. `needs:review`는 담당자 검토가 필요한 항목에 사용한다. 라벨은 등록 시 생성한다.

## ISS-001 · 착수 범위와 운영 전제 확정

- Milestone: M1 · Labels: `priority:P0`, `type:decision`, `needs:review`
- 선행: 없음 · 기획서: §1~2, §9, §12, §14

최초 업종·시설, 소방 검토 담당자·일정, 자료 형식·수량, API 신청 상태, 클라우드 소유 계정·도메인·기관 요구사항, 부가세·운영비 포함 여부와 유지보수 범위를 결정 기록으로 관리한다.

### 결정 기록 · 2026-09-17

- 최초 지원 범위: 렛츠코딩앤플레이 학원.
- 소방 담당 검토자: 일을 의뢰한 의뢰자.
- 법령 API 키: 로컬 `.env`의 `API_AUTHKEY`에 저장됨.
- OpenRouter 개발용 키: 로컬 `.env`의 `OPENROUTER_APIKEY`에 저장됨. 구현 전 `OPENROUTER_API_KEY`로 이름을 통일하거나 환경변수 매핑을 확정한다. 비밀값은 문서와 GitHub에 기록하지 않는다.
- Supabase 프로젝트: `FireChatbot` (`voiiciyuotuyejcoysbh`, `https://voiiciyuotuyejcoysbh.supabase.co`)를 사용한다. CLI 로그인과 활성 상태를 확인했다. Publishable/Secret key 입력은 [Supabase 환경변수 설정](supabase-environment-setup.md)을 따른다.
- Vercel CLI: `turboguy36-9404` 계정으로 로그인 확인 완료.

- [ ] 각 항목의 결정값 또는 미확정 상태·담당자·기한·차단 범위를 기록했다.
- [ ] 공개 법령 PoC 범위와 문서 영구 ID 수집 목록 초안을 선정했다.
- [ ] 자료 공개와 외부 모델 전송 승인을 구별하고 미승인 내부 자료를 개발에 사용하지 않는다.
- [ ] 일반음식점·8주·1인·2천만원의 확정 여부와 범위 변경 처리 방식을 기록했다.

## ISS-002 · Next.js 개발 기반과 환경별 설정 구성

- Milestone: M1 · Labels: `priority:P0`, `type:feature`, `area:platform`
- 선행: 없음 · 기획서: §3, §9, §13

Next.js App Router·TypeScript·Node.js 서버 런타임, 제안 디렉터리, 환경 검증과 기본 CI를 구성한다.
- [ ] 로컬 실행·타입 검사·빌드 절차와 CI가 동작한다.
- [ ] 기획서 환경변수의 값 없는 예시와 서버/브라우저 구분을 문서화했다.
- [ ] 개발·Preview·운영 키 분리와 비밀값 저장소 제외, OC·Authorization 로그 마스킹을 적용했다.
- [ ] Supabase 연결 및 Vercel Preview 배포를 확인하고 직접 DB 연결 사용 시 풀링을 설정했다.

## ISS-003 · 법령 API 실제 응답 및 권한 PoC

- Milestone: M1 · Labels: `priority:P0`, `type:spike`, `area:ingestion`
- 선행: ISS-002; API 신청·권한 확보 · 기획서: §4.1~4.3

법령 목록·본문, NFPC·NFTC 본문·첨부, 시행령 별표 4, 소방청 해석을 실제 호출하고 인증값을 제거한 fixture를 확보한다.
- [ ] HTTPS·JSON·권한을 확인하고 상태 코드 외 Content-Type·오류 본문·결과 수·ID·실제 본문을 검사했다.
- [ ] 법령/행정규칙의 현행 값, ID/MST/목록 시행일을 구분해 조회했다.
- [ ] 개발과 Preview 결과를 기록하고 IP 제약을 확인했다. 고정 IP가 요구되면 대응안을 기록했다.
- [ ] HTTP 200 오류 HTML·빈 응답·스키마 불일치를 성공으로 처리하지 않는 계약 검증 자료가 있다.

## ISS-004 · OpenRouter 모델과 임베딩 후보 PoC

- Milestone: M1 · Labels: `priority:P0`, `type:spike`, `area:chat`
- 선행: ISS-002; OpenRouter 키·한도 확보 · 기획서: §6.1, §6.3~6.4, §12

후보 챗 모델 2~3개와 다국어 임베딩을 실제 연결해 한국어·JSON·모름 응답·표 조건·지연·비용을 비교한다.
- [ ] JSON Schema 및 `provider.require_parameters=true` 지원을 실제 확인했다.
- [ ] 기본·대체 모델의 평가 결과와 허용 목록을 기록했다.
- [ ] 임베딩 모델 ID·revision·차원·현재 사용 가능 여부와 단가 확인일을 기록했다.
- [ ] 토큰·비용·실패 유형 측정 방법과 요청 한도 초안을 정했다.

## ISS-005 · Supabase 스키마와 접근 정책 마이그레이션

- Milestone: M2 · Labels: `priority:P0`, `type:feature`, `area:data`, `area:security`
- 선행: ISS-002, ISS-003 · 기획서: §5, §9

기획서 §5의 문서·버전·조항·검색·관계·파일·규칙·사례·대화·답변 실행·작업·검토·피드백 테이블과 Storage 정책을 구현한다.
- [ ] 빈 환경에 마이그레이션을 적용하고 UUID/원천 ID 분리, 고유 제약·외래키를 검증했다.
- [ ] 원천 특성에 맞는 버전 키와 내부 파일 hash revision을 적용했다.
- [ ] 공개 데이터와 운영 데이터의 grants·RLS·Storage 권한을 분리했다.
- [ ] secret 키를 쓰는 서버도 소유권·관리자 검사를 거치며 검색 RPC에서 공개 범위를 강제한다.

## ISS-006 · target별 법령 API 어댑터 구현

- Milestone: M2 · Labels: `priority:P0`, `type:feature`, `area:ingestion`
- 선행: ISS-003 · 기획서: §4.2~4.4

`eflaw`, `admrul`, `licbyl`, `admbyl`, `nfaCgmExpc`의 목록·본문 지원 계약을 개별 어댑터와 런타임 스키마로 구현한다.
- [ ] target별 필드·현행 값·식별자·시행일을 독립 처리한다.
- [ ] 전체 페이지를 순회하고 `totalCnt`와 수집 건수를 대조한다.
- [ ] 실제 fixture의 정상·오류 HTML·빈 본문·형태 차이를 계약 테스트로 검증했다.
- [ ] 인증 오류와 재시도 가능한 일시 오류를 구별하고 비밀값을 기록하지 않는다.

## ISS-007 · 원문 수집 CLI와 멱등 저장 구현

- Milestone: M2 · Labels: `priority:P0`, `type:feature`, `area:ingestion`
- 선행: ISS-005, ISS-006 · 기획서: §4.4, §9, §13

선정 영구 ID 목록을 기준으로 탐색→버전 본문→원문 Storage 저장을 실행하는 CLI와 진행 기록을 만든다.
- [ ] 선정 문서 본문·부칙·필수 별표 수집 결과 및 누락 목록을 출력한다.
- [ ] 문서 ID·버전·hash로 중복을 막고 같은 원문은 재처리 대상으로 만들지 않는다.
- [ ] 상대 첨부 링크를 공식 호스트로 해석하고 호스트·크기·형식을 제한한다.
- [ ] 실패 후 재실행해 완료된 원문을 중복 게시하지 않고 실패 항목을 복구했다.

## ISS-008 · 조항·별표 정규화 및 버전 보존 구현

- Milestone: M2 · Labels: `priority:P0`, `type:feature`, `area:ingestion`
- 선행: ISS-007 · 기획서: §4.4~4.5, §5

조문·항·호·기준 번호·부칙·별표 구조와 locator를 보존하고 법령 별표/행정규칙 첨부 파서를 분리한다.
- [ ] 분할 항목에 상위 제목·조건·비고를 연결하고 원문 위치를 재조회할 수 있다.
- [ ] 불완전한 표를 `needs_review`로 표시하고 자동 판단용 게시를 차단한다.
- [ ] 현행·시행예정·연혁을 구별하고 신규 버전이 구버전을 덮어쓰지 않는다.
- [ ] 대표 조항·표·부칙 fixture에서 추출 및 버전 보존을 검증했다.

## ISS-009 · 임베딩 및 검색 인덱스 파이프라인 구현

- Milestone: M3 · Labels: `priority:P0`, `type:feature`, `area:search`
- 선행: ISS-004, ISS-008 · 기획서: §5, §6.1

문맥을 보존한 chunk와 pg_trgm·pgvector 인덱스를 구성하고 승인 데이터 게시 절차를 마련한다.
- [ ] chunk hash와 모델/revision으로 변경 항목만 임베딩한다.
- [ ] 질의·문서 모델과 차원을 검사하고 불일치를 차단한다.
- [ ] 모델 변경 시 별도 인덱스 구축·전환·복귀 절차가 있다.
- [ ] 관리자 UI 전에도 담당자 승인 증거를 남기는 제한된 CLI/관리 절차로 평가 corpus를 게시할 수 있다.

## ISS-010 · 버전·권한 필터를 포함한 하이브리드 검색 구현

- Milestone: M3 · Labels: `priority:P0`, `type:feature`, `area:search`
- 선행: ISS-009 · 기획서: §6.1

법령명·NFPC/NFTC 코드·조항 정확 일치와 한국어 키워드·벡터 검색을 결합하고 RRF 등으로 순위를 합친다.
- [ ] 기준일·버전·게시 상태·공개 범위를 검색 단계에서 강제한다.
- [ ] 상위 제목·비고·관련 예외를 확장하고 최종 근거에 포함한다.
- [ ] 후보 30개·최종 6~10개를 초기값으로 두고 평가 결과에 따라 조정 가능하다.
- [ ] 근거 부족·기준일 불명확을 구조화된 상태로 반환한다.

## ISS-011 · 버전 고정 근거 조회 API 및 근거 카드 구현

- Milestone: M3 · Labels: `priority:P0`, `type:feature`, `area:search`
- 선행: ISS-005, ISS-008 · 기획서: §2.1, §6.3, §8

`GET /api/sources/:id`와 원문 제목·시행일·조항/별표 위치·버전을 보여주는 재사용 카드를 구현한다.
- [ ] 서버가 DB 출처로 URL을 구성하고 접근 가능한 원문만 반환한다.
- [ ] 구버전 ID로 과거 답변의 동일 원문을 재조회한다.
- [ ] 미존재·비공개·미승인 자료 접근을 정책대로 차단한다.
- [ ] 카드에서 출처와 시행일·locator를 확인하고 원문으로 이동할 수 있다.

## ISS-012 · 담당자 검색 평가셋과 기준 성능 작성

- Milestone: M3 · Labels: `priority:P0`, `type:test`, `area:search`, `needs:review`
- 선행: ISS-010, ISS-011; 담당자 정답 검토 · 기획서: §6.1, §10

질문 50건 이상과 필수 근거 ID·버전·기준일을 확보하고 반복 가능한 Recall@10 평가를 구성한다.
- [ ] 명칭·코드·별표·예외·불충분 정보 사례와 담당자 검토 상태를 포함했다.
- [ ] 정확 일치·키워드·벡터 및 결합 검색을 비교했다.
- [ ] 데이터/인덱스 버전, 측정 방식, 실패 유형과 개선 목록을 기록했다.
- [ ] 90% 목표 대비 기준 결과를 기록하고 미달 개선 작업을 ISS-028에 연결했다.

## ISS-013 · 서명 세션·요청 검증·중복 요청 방지 구현

- Milestone: M4 · Labels: `priority:P0`, `type:feature`, `area:security`
- 선행: ISS-002, ISS-005 · 기획서: §5, §6.2, §8~9

일반 사용자 서명 익명 세션, 관리자 Supabase Auth 기반 권한 공통 모듈과 요청 검증을 구현한다.
- [ ] 대화·사례에 소유권 검사를 적용하고 입력 길이·enum·페이지 크기를 제한한다.
- [ ] 세션별 `clientRequestId` 고유 키로 동시·재전송 중복 생성과 중복 과금을 방지한다.
- [ ] 요청 처리 중·완료·실패 상태별 재요청 동작을 정의했다.
- [ ] 위조 세션·타 세션 접근·동일 요청 동시 실행을 검증했다.

## ISS-014 · OpenRouter 구조화 생성 및 응답 검증 구현

- Milestone: M4 · Labels: `priority:P0`, `type:feature`, `area:chat`
- 선행: ISS-004, ISS-010 · 기획서: §6.3~6.4

서버 모델 호출, `ChatAnswer`/`AnswerEnvelope` 스키마, 허용 근거 ID 검증과 안전한 실패 응답을 구현한다.
- [ ] 질문·확인된 조건·규칙 결과·허용 근거만 전달하고 원문 지시를 시스템 지시로 해석하지 않는다.
- [ ] LLM이 판정·URL을 새로 만들지 않으며 적용 결론·숫자·조건은 서버 규칙 템플릿으로 결합한다.
- [ ] 구조/인용 검증 실패 시 1회 재시도 후 근거 목록과 추가 확인 안내를 반환한다.
- [ ] 미등록 인용·잘못된 JSON·근거 내 지시문 사례를 검증했다.

## ISS-015 · 채팅 API와 답변 실행 추적 구현

- Milestone: M4 · Labels: `priority:P0`, `type:feature`, `area:chat`
- 선행: ISS-011, ISS-013, ISS-014 · 기획서: §5, §6.2~6.4, §8

`POST /api/chat`에 인증·한도·검색·생성·검증·저장을 연결하고 SSE 상태 및 최종 답변을 제공한다.
- [ ] SSE는 검색/작성 상태와 검증된 최종 답변만 전송한다.
- [ ] 모델·프롬프트·규칙·corpus 버전, 근거 ID, 입력 snapshot, case revision, 토큰·비용·지연을 기록한다.
- [ ] 일반 법령 질문에 건물 조건을 강제하지 않고 무근거 응답은 추가 확인으로 처리한다.
- [ ] 질문에서 인용 답변까지 핵심 통합 흐름을 검증했다.

## ISS-016 · 법령 질문 화면과 사용자 오류 신고 구현

- Milestone: M4 · Labels: `priority:P0`, `type:feature`, `area:chat`
- 선행: ISS-015 · 기획서: §2, §8

첫 화면의 법령 질문/영업장 확인 진입점, 채팅·근거 카드·추가 질문·`POST /api/feedback`을 구현한다. 영업장 판정 활성화는 M5 완료 조건을 따른다.
- [ ] 진행·최종·근거 부족·오류 상태를 구별하고 실패 후 재질문할 수 있다.
- [ ] 답변 오류 신고가 해당 답변 실행과 연결되며 소유권을 검사한다.
- [ ] 검색 안내와 승인된 시설 적용 판단을 구별해 표시한다.
- [ ] 4주차 제품 검토를 수행하고 피드백·수정 내역을 기록했다.

## ISS-017 · 최초 업종 판단표와 정답 사례 승인

- Milestone: M5 · Labels: `priority:P0`, `type:decision`, `area:rules`, `needs:review`
- 선행: ISS-001, ISS-008 · 기획서: §4.5, §7, §10

담당자가 최초 지원 업종·시설별 입력·경계값·예외·부칙·사건 날짜와 정확한 근거 버전을 검토한다.
- [ ] 시행령 별표 2·4·5 및 관련 NFPC/NFTC의 필요한 근거를 판단표에 연결했다.
- [ ] 필수 입력과 해당/비해당/추가 확인 기대 결과를 사례별 작성했다.
- [ ] 경계값·누락·예외·기존/신축/용도변경 사례를 포함했다.
- [ ] 승인자·범위·규칙 버전·검토일을 기록하거나 미승인 상태로 공개 판정을 차단했다.

## ISS-018 · 영업장 조건 상태와 revision API 구현

- Milestone: M5 · Labels: `priority:P0`, `type:feature`, `area:rules`
- 선행: ISS-013, ISS-017 · 기획서: §6.2, §7~8

`POST /api/cases`, `PATCH /api/cases/:id`와 자연어 추출 후보·확인 상태·추가 질문을 구현한다.
- [ ] 필드별 `unknown/extracted/user_confirmed`와 출처 메시지·단위·사건 날짜를 저장한다.
- [ ] 애매한 값을 임의 확정하지 않고 현재 규칙에 필요한 항목만 질문한다.
- [ ] 조건 변경 시 revision 증가·판단 캐시 무효화 및 낙관적 수정 충돌 감지가 동작한다.
- [ ] 타 세션 수정과 이전 revision 덮어쓰기를 차단한다.

## ISS-019 · 승인된 선언형 규칙 실행기 구현

- Milestone: M5 · Labels: `priority:P0`, `type:feature`, `area:rules`
- 선행: ISS-017, ISS-018 · 기획서: §4.5, §6.2~6.3, §7

JSON 또는 TypeScript 규칙과 특정 원문 버전 연결을 구현하고 임의 문자열 `eval` 없이 실행한다.
- [ ] 검사 입력·미확인 조건·예외 결과·근거·검토자·규칙 버전을 반환한다.
- [ ] 승인 범위에서만 해당/비해당을 만들며 검색 부재·미지원·미확인 조건을 비해당으로 처리하지 않는다.
- [ ] 기준일과 건물 사건 날짜를 구별하고 부칙 판단이 불명확하면 추가 확인으로 낮춘다.
- [ ] 승인 정답 사례의 경계값·예외·입력 누락 테스트가 모두 일치한다.

## ISS-020 · 영업장 조건 카드와 시설 적용 결과 연결

- Milestone: M5 · Labels: `priority:P0`, `type:feature`, `area:rules`
- 선행: ISS-016, ISS-018, ISS-019 · 기획서: §2, §6.2~6.3, §8

영업장 확인 화면에서 후보 확인·수정·추가 질문과 서버 판단 결과를 채팅에 연결한다.
- [ ] 시설별 해당/비해당/추가 확인을 AI 설명과 별도 표로 보여준다.
- [ ] 규칙 없는 범위는 검색 안내만 제공하고 판정 완료로 표시하지 않는다.
- [ ] 조건 수정 뒤 새 revision에 맞춘 결과와 근거를 표시한다.
- [ ] 담당자 선정 사례에서 입력부터 결과·근거까지 재현하고 규칙/설명 충돌이 없다.

## ISS-021 · 관리자 문서·규칙 승인과 답변 추적 화면 구현

- Milestone: M6 · Labels: `priority:P0`, `type:feature`, `area:admin`
- 선행: ISS-013, ISS-019 · 기획서: §5, §8

승인 대기·문서 비교·규칙 근거·답변 실행 조회 및 `POST /api/admin/reviews/:id`를 구현한다.
- [ ] 관리자만 승인/반려하며 대상 버전·검토자·시각·이유를 감사 이력에 남긴다.
- [ ] 파싱 불완전·근거 미승인 상태의 게시를 차단한다.
- [ ] 사용자 신고 처리 상태와 해당 답변의 모델·입력·규칙·근거를 추적한다.
- [ ] 승인 범용 편집기 없이 승인/반려 흐름으로 운영 가능하다.

## ISS-022 · 개정 감지와 영향 규칙 무효화 구현

- Milestone: M6 · Labels: `priority:P0`, `type:feature`, `area:ingestion`
- 선행: ISS-008, ISS-019, ISS-021 · 기획서: §4.5

선정 문서 새 버전 발견과 조항 차이·연결 규칙 영향 분석 및 재검토 흐름을 구현한다.
- [ ] 구버전과 과거 답변은 보존하고 신규 버전을 별도로 등록한다.
- [ ] 영향 규칙은 검토 대기로 전환되며 기존 판단 캐시도 무효화한다.
- [ ] 승인 전 영향받은 자동 판정은 추가 확인으로 반환한다.
- [ ] 새 버전 발견→차이 확인→재승인→판단 재개 흐름을 검증했다.

## ISS-023 · lease 기반 작업 큐와 예약 수집 운영 구현

- Milestone: M6 · Labels: `priority:P0`, `type:feature`, `area:ingestion`
- 선행: ISS-007, ISS-013 · 기획서: §3, §8~9

관리자 작업 등록·상태 조회 API와 인증된 `GET /api/cron/ingestion`의 작은 배치 실행을 구현한다.
- [ ] 원자적 claim·lease 만료·attempts·next_run_at·cursor·오류를 저장한다.
- [ ] 실행시간 예산 전에 종료하고 다음 호출에서 재개한다.
- [ ] 중복 Cron·강제 중단·재시도 후에도 동일 버전이 중복 게시되지 않는다.
- [ ] 관리자가 진행·실패 항목을 조회하고 재처리하며 Cron 자동 재시도를 전제하지 않는다.

## ISS-024 · 소방청 해석 및 내부 자료 공개·전송 승인 구현

- Milestone: M6 · Labels: `priority:P1`, `type:feature`, `area:admin`, `needs:review`
- 선행: ISS-001, ISS-006, ISS-021 · 기획서: §4.1, §5, §9

선정 해석과 제공 내부 자료를 source type·분류·출처·파일 revision으로 관리하고 공개/전송 권한을 검색 및 모델 전달에 적용한다.
- [ ] 해석의 질의요지·회답·이유와 담당자 선정 여부를 보존한다.
- [ ] 내부 자료 공개 승인과 외부 모델 전송 승인을 별도 저장·검사한다.
- [ ] 미승인 자료가 검색 응답·원문 조회·모델 요청에 유출되지 않는다.
- [ ] 실제 내부 자료가 없거나 미승인이면 비식별 fixture로 기능을 검증하고 미투입 상태를 기록한다.

## ISS-025 · 세션·관리자·내부 자료 권한 통합 검증

- Milestone: M7 · Labels: `priority:P0`, `type:test`, `area:security`
- 선행: ISS-020, ISS-021, ISS-023, ISS-024 · 기획서: §5, §8~10

서버 소유권·RLS/grants·검색 RPC·Storage·Cron 인증과 로그 마스킹을 교차 검증한다.
- [ ] 타 세션 대화/사례/신고·비공개 문서·관리자 API·Cron의 비인가 접근을 차단했다.
- [ ] secret 키 경로에서도 권한 검사를 우회할 수 없다.
- [ ] OC·Authorization·서비스 비밀값이 로그와 브라우저 번들에 없다.
- [ ] 발견 결함을 수정하고 재현 시나리오 및 검증 증거를 기록했다.

## ISS-026 · 모델 장애 복구와 요청·비용 제한 구현

- Milestone: M7 · Labels: `priority:P0`, `type:feature`, `area:chat`
- 선행: ISS-015, ISS-023 · 기획서: §6.4, §9~10

429/5xx 제한된 지수 백오프, 평가 통과 대체 모델, 인증/잔액 오류 분류 및 서버 예산 집행을 완성한다.
- [ ] 인증 오류·잔액 부족은 반복 호출하지 않고 운영 오류로 기록한다.
- [ ] 모델 실패 시 확보된 근거를 보여주고 새 결론을 추정하지 않는다.
- [ ] 요청 토큰·세션 횟수·일일 비용 상한을 동시 요청에서도 집행한다.
- [ ] 429·5xx·인증 실패·잘못된 JSON·수집 중단을 주입해 제한된 복구 동작을 확인했다.

## ISS-027 · 호출량·비용·실패 지표와 성능 측정

- Milestone: M7 · Labels: `priority:P1`, `type:ops`, `area:platform`
- 선행: ISS-021, ISS-026 · 기획서: §2.1, §10, §12

관리자 운영 지표, 비용 경보와 100개 질문 부하·비용 측정 보고서를 작성한다.
- [ ] 호출량·토큰·모델 비용·검색 실패·지연·신고 상태를 조회할 수 있다.
- [ ] 100개 질문의 평균/p95 토큰·비용·최종 답변 지연과 측정 환경을 기록했다.
- [ ] 대화 문맥·재시도·임베딩을 포함해 월 1,000/10,000/50,000회 비용을 환산했다.
- [ ] 클라우드·DB·스토리지·트래픽 비용 가정과 단가 확인일, 비용 상한·경보 기준을 기록했다.

## ISS-028 · 검색·규칙·인용 품질 및 핵심 E2E 최종 검증

- Milestone: M7 · Labels: `priority:P0`, `type:test`, `needs:review`
- 선행: ISS-012, ISS-020, ISS-022, ISS-025, ISS-026, ISS-027 · 기획서: §10

담당자 정답 기준으로 수집 완전성·버전·검색·규칙·인용·정보 부족과 핵심 사용자 흐름을 검수한다.
- [ ] 선정 본문·부칙·필수 별표 누락과 현행/시행예정 혼용이 없고 과거 원문을 재조회한다.
- [ ] 50건 이상 Recall@10 90% 목표, 승인 규칙의 경계·예외·누락 기대 결과 전부 일치를 확인했다.
- [ ] 검수 사례에서 존재하지 않거나 접근 불가한 인용 및 핵심 결론/규칙 충돌이 0건이다.
- [ ] 정보 부족 시 추가 질문, 조건 수정, 근거 조회, 개정 재검토의 핵심 E2E를 통과했다.
- [ ] p95 20초 목표와 권한·복구 결과를 포함한 검수 보고서에 미달·수정·재검증을 기록했다.

## ISS-029 · 운영 정책·계정·지원 범위 확정

- Milestone: M8 · Labels: `priority:P0`, `type:decision`, `needs:review`
- 선행: ISS-001, ISS-024, ISS-027 · 기획서: §9, §12, §14

운영 배포를 막는 미확정 항목을 해소하고 승인된 지원 범위 및 운영 책임을 확정한다.
- [ ] 프로젝트 소유 계정·도메인·기관 배포 요구사항과 운영 담당자가 확정되었다.
- [ ] 대화/원문 저장기간·삭제 정책과 내부 자료 공개/전송 정책을 승인했다.
- [ ] 부가세·운영비·월 한도·유지보수·장애 연락 및 검토 책임을 기록했다.
- [ ] 승인 업종·시설·규칙과 미지원 범위를 운영 안내에 명시했다.

## ISS-030 · 운영 배포·백업 복원·롤백 검증

- Milestone: M8 · Labels: `priority:P0`, `type:ops`, `area:platform`
- 선행: ISS-028, ISS-029 · 기획서: §9, §11

Vercel/Supabase 운영 환경에 승인된 앱·스키마·corpus·규칙을 배포하고 복구 절차를 실행한다.
- [ ] 운영 키·도메인·Cron·한도·경보를 설정하고 개발/Preview와 분리했다.
- [ ] 배포 후 질문·근거·승인 판단·관리자·갱신 작업을 점검했다.
- [ ] DB/Storage 백업 및 복원 절차를 검증하고 앱·인덱스·규칙 롤백 경로를 기록했다.
- [ ] 배포 버전·설정 변경·검증 결과·운영 책임자를 기록했다.

## ISS-031 · 사용자·관리자 안내 및 운영 인수인계 문서 작성

- Milestone: M8 · Labels: `priority:P0`, `type:docs`
- 선행: ISS-021, ISS-023, ISS-029, ISS-030 · 기획서: §11, §13

사용자 이용 방법과 개발/수집/검토/개정/장애/복구 운영 문서를 작성하고 담당자에게 설명한다.
- [ ] 질문·조건 수정·출처 확인·추가 확인 의미·오류 신고 이용 안내가 있다.
- [ ] 수집 실행·실패 재처리·문서/규칙 승인·개정 영향 검토 절차가 있다.
- [ ] 환경 설정·마이그레이션·모델/인덱스 교체·비용 경보·백업 복원·롤백 절차가 있다.
- [ ] 비밀값 없이 계정/권한 인계 방법과 교육·질의응답 기록을 남겼다.

## ISS-032 · 담당자 최종 검수 및 출시 결과 기록

- Milestone: M8 · Labels: `priority:P0`, `type:test`, `needs:review`
- 선행: ISS-028, ISS-030, ISS-031 · 기획서: §10~11

운영 환경에서 최종 검수를 수행하고 실제 출시 범위와 인수 결과를 확정한다.
- [ ] 담당자가 핵심 사례의 입력·판단·근거·버전 재현을 확인했다.
- [ ] 검수 기준별 결과·증거·검토자·일자·남은 결함 처리 결정을 기록했다.
- [ ] 미승인 규칙은 공개하지 않고 실제 제공 가능한 검색/판단 범위를 확정했다.
- [ ] 인수 확인과 유지보수 인계, 별도 후속 범위 백로그를 기록했다.
